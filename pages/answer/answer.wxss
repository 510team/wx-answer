.answers {
  padding: 0 5%;
  font-size: 12px;
  position: relative;
}
.alarm {
  position: fixed;
  top: 20px;
  right: 20px;
  height: 32px;
  line-height: 32px;
  padding: 0 20px;
  border-radius: 20px;
  background: rgba(0, 0, 0, 0.7);
  color: #fff;
  z-index: 100;
}
.questions-box {
  padding-top: 72px;
}
.question {
  line-height: 24px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 20px;
  padding: 20px;
  font-size: 14px;
}
.answer-box {
  margin-top: 10px;
  position: relative;
}
.answer-box .icon {
  position: absolute;
  width: 20px;
  height: 20px;
  margin-top: 5px;
}
.answer-box button {
  border: 0px;
  padding: 0;
  display: block;
  line-height: 30px;
  margin-left: 28px;
  text-align: left;
  margin-bottom: 10px;
  font-size: 14px;
  background: none;
  border-radius: 0px;
  border-bottom: 1px dashed #5ca05f;
  padding-left: 3px;
  color: rgba(0, 0, 0, 0.7);
}
/* 去掉默认边框 */
.btn::after {
  border: 0;
}
/* 禁用的样式 */
button[disabled][type="default"],
wx-button[disabled]:not([type]) {
  color: rgba(0, 0, 0, 0.3);
  background: none;
}
.answer-box button.btn_hover,
.answer-box .cur-btn {
  color: #5ca05f;
}

.right-box,
.wrong-box {
  position: fixed;
  text-align: center;
  left: 0;
  top: 0;
  transform: translate(0, 200%) scale(0);
  opacity: 0;
  z-index: 100;
}
